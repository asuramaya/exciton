{
  "name": "exciton-edge",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker serving exciton's public read API + HMAC-gated publish endpoint.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
